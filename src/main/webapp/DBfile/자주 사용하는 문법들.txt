[JAVA]

다형성 : 부모에 자식을 넣는다는 의미 . 자식의 오버라이드된 메서드가 호출된다

[JavaScript] 

window.open('링크 주소);

[JSP]

절대 참조시 컨텍스트 경로가 자동으로 추가되는경우

1) web.xml 

2) include 

3) forward

SERVER.XML => 톰캣이 제공하는 커넥션 풀(DBCP => DATABASE CONNECTION POOL)을 사용하기 위해선 

RESOURCE 작성 

[모델2] MVC 패턴 => GUI 기반의 어플리케이션을 구현하기 위한 디자인 패턴

사용자(클라이언트)에게 컨트롤러에게 요청하면 컨트롤러가 모델에게 비즈니스 로직을 요청 후 결과를 받아
뷰에게 포워드하며 뷰가 클라이언트에게 결과를 보여줌

//1. HTTP 요청받음

//2. 클라이어트가 요구하는 기능 분석

//3.

//4

대부분 모델은 평이한 일반 자바 클래스(POJO : plain old java object) => 상속이나 구현을 하지않는 자바 클래스